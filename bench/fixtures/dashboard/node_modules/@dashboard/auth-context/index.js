'use client'

import { createContext, createElement } from 'react'

export const DashboardSessionContext = createContext(null)
export function DashboardSessionProvider({ session, children }) {
  return createElement(DashboardSessionContext.Provider, { value: session }, children)
}
