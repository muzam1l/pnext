import { decodeSession } from '@dashboard/auth-crypto'

export function loadSession(cookie) {
  const decoded = decodeSession(cookie)
  return decoded ? { ...decoded, role: 'admin' } : { subject: 'guest', role: 'viewer' }
}
